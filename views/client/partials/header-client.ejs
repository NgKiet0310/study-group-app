<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="/home">Study Group📚</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto align-items-center">
        <li class="nav-item">
          <a class="nav-link" href="/home">Home</a>
        </li>
        <% if (user) { %>
          <li class="nav-item">
            <a class="nav-link">Welcome, <span class="badge bg-light text-dark"><%= user.username %></span></a>
          </li>
          <li class="nav-item">
      <a class="nav-link btn btn-light text-danger ms-2" href="/auth/logout" onclick="return confirm('Bạn chắc chắn muốn đăng xuất?')">Đăng xuất</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link btn btn-light text-primary ms-2" href="/auth/login">Login</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>